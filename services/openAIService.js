const fs = require('fs').promises;

const generateDocument = async (text, documentType, imageFile) => {
  try {
    if (imageFile) {
      await fs.unlink(imageFile.path);
    }

    return getMockResponse(text, documentType, imageFile);

  } catch (error) {
    console.error('Mock OpenAI service error:', error);
    throw error;
  }
};

const getMockResponse = (text, documentType, imageFile) => {
  const hasImage = imageFile ? true : false;
  const imageSection = hasImage ? '\n\n## 画像について\n提供された画像を参考に設計を行いました。' : '';

  const mockResponses = {
    specification: `# 仕様書

## 1. 概要
${text}に基づくシステムの仕様書です。

## 2. 機能要件
### 2.1 基本機能
- ユーザー入力処理機能
- データ保存・取得機能
- 結果表示機能

### 2.2 詳細機能
- 入力値バリデーション
- エラーハンドリング
- レスポンシブ対応

## 3. 非機能要件
### 3.1 性能要件
- レスポンス時間: 3秒以内
- 同時接続数: 100ユーザー

### 3.2 セキュリティ要件
- 入力値サニタイゼーション
- HTTPS通信必須

## 4. 画面設計${imageSection}
### 4.1 メイン画面
- 入力フォーム
- 結果表示エリア
- エラーメッセージ表示

## 5. データ設計
### 5.1 入力データ
- テキスト: 文字列型（最大500文字）
- 画像: ファイル型（任意）
- 文書種類: 列挙型

### 5.2 出力データ
- 生成文書: 文字列型
- ステータス: 真偽値型

## 6. 制約事項
- ブラウザ対応: Chrome, Firefox, Safari最新版
- ファイルサイズ制限: 5MB以下
- 文字数制限: 500文字以内`,

    design: `# 設計書

## 1. システム構成
${text}に基づくシステムアーキテクチャの設計書です。

## 2. アーキテクチャ設計
### 2.1 全体構成
- フロントエンド: HTML/CSS/JavaScript
- バックエンド: Node.js + Express
- データベース: 不要（ステートレス）

### 2.2 コンポーネント構成
- プレゼンテーション層
- ビジネスロジック層
- データアクセス層

## 3. データベース設計
### 3.1 論理設計
現在のシステムではデータベースは使用しませんが、将来的な拡張を考慮した設計を提案します。

### 3.2 物理設計
- テーブル設計（将来用）
- インデックス設計（将来用）

## 4. API設計${imageSection}
### 4.1 エンドポイント
- GET /: メインページ表示
- POST /api/generate: 文書生成

### 4.2 リクエスト/レスポンス形式
- Content-Type: multipart/form-data
- レスポンス: JSON形式

## 5. セキュリティ設計
### 5.1 認証・認可
- 現バージョンでは認証なし
- 将来的にはJWT認証を検討

### 5.2 データ保護
- 入力値サニタイゼーション
- ファイルアップロード制限

## 6. 実装方針
### 6.1 開発環境
- Node.js v18以上
- npm パッケージ管理

### 6.2 デプロイ方針
- Docker化対応
- CI/CD パイプライン構築`,

    confirmation: `# 確認書

## 1. 確認事項一覧
${text}に関する確認書です。

### 1.1 機能確認項目
- [ ] テキスト入力機能の動作確認
- [ ] 画像アップロード機能の動作確認
- [ ] 文書生成機能の動作確認
- [ ] エラーハンドリングの動作確認

### 1.2 UI/UX確認項目
- [ ] レスポンシブデザインの確認
- [ ] ブラウザ互換性の確認
- [ ] アクセシビリティの確認

## 2. テストケース${imageSection}
### 2.1 正常系テスト
| No | テスト項目 | 入力値 | 期待結果 |
|----|-----------|--------|----------|
| 1 | 仕様書生成 | 有効なテキスト | 仕様書が生成される |
| 2 | 設計書生成 | 有効なテキスト | 設計書が生成される |
| 3 | 確認書生成 | 有効なテキスト | 確認書が生成される |

### 2.2 異常系テスト
| No | テスト項目 | 入力値 | 期待結果 |
|----|-----------|--------|----------|
| 1 | 空文字入力 | 空文字 | エラーメッセージ表示 |
| 2 | 文字数超過 | 501文字以上 | エラーメッセージ表示 |
| 3 | 不正ファイル | 画像以外のファイル | エラーメッセージ表示 |

## 3. 受入基準
### 3.1 機能要件
- 全ての文書タイプで適切な文書が生成されること
- 画像アップロード機能が正常に動作すること
- バリデーション機能が適切に動作すること

### 3.2 非機能要件
- レスポンス時間が3秒以内であること
- 主要ブラウザで正常に動作すること

## 4. リスク事項
### 4.1 技術的リスク
- ブラウザ互換性の問題
- ファイルアップロードのセキュリティリスク

### 4.2 運用リスク
- サーバー負荷の増大
- 不正利用の可能性

## 5. 確認スケジュール
### 5.1 テスト実施予定
- 単体テスト: 開発完了後1日
- 結合テスト: 単体テスト完了後2日
- 受入テスト: 結合テスト完了後1日

### 5.2 リリース予定
- ステージング環境: テスト完了後1日
- 本番環境: ステージング確認後1日`
  };

  return mockResponses[documentType] || mockResponses.specification;
};

module.exports = {
  generateDocument
};
